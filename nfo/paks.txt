// My packages file
// vim: ft=randypacks
// Comments are C++-like

First to install:

   vi                // VI VI VI - evil power editor
   sudo              // Super user do
   screen            // Terminal multiplexer
   bash-completion   // Better completion on bash
   pkgfile           // For command not found hook (see setup (1))
   openssh           // SSH server (see setup)

UEFI boot:

   efibootmgr
   gummiboot

Packaging:

   aur/yaourt   // Aur "pacman"
   namcap         // Package checking

Video:

   intel-dri
   xf86-video-intel
   libva-intel-driver
   mesa-demos          // Beloved glxgears is here

Xorg stuff:

   xorg-server
   xorg-xinit

   xf86-input-wacom
   xorg-xev
   xorg-xprop    // Helper utility for windows info
   xvinfo        // Helper for lightson script
   xclip         // For scripting with clipboard

Diagnostics:

   htop-vi  // Same as htop, but has vi keys!
   sysdig

Window manager and DE:

   awesome
   aur/compton-git   // composition manager
   libnotify         // notifications

   //parcellite    // clipboard manager good and lite // Has problems with nautilus Cut/Copy/Paste

Terminal emulators:

   rxvt-unicode   // Default terminal, using urxvt{d,c} pair
   aur/st         // JIC fallback
   xterm          // Another JIC term ;)

Console tools:

   zsh
   zsh-syntax-highlighting

   unp            // Console universal unpacker
   par2cmdline    // Par2 utility
   aur/rmlint         // Duplicate files searcher and remover

   pv             // echo "You can simulate on-screen typing just like in the movies" | pv -qL 10

Nautilus:

   nautilus                // Big slow but powerful (see setup(3))
   libnautilus-extension
   python2-nautilus

   nautilus-actions        // For custom actions in context menus
   nautilus-open-terminal  // To open current dir in TERM
   nautilus-compare        // Integrates meld into context menu
   nautilus-sendto         // Send to pidgin and other places

   sushi                   // Quick preview on space
   baobab                  // Visual disk space usage

   gvfs           // GNOME file system abstraction
   gvfs-smb       // Samba support
   gvfs-mtp       // Android devices MTP mode support
   gvfs-afc
   gvfs-afp
   gvfs-gphoto2
   libgphoto2

   dosfstools           // For old FAT etc
   reiserfsprogs        // Reiser formatting / mounting
   gptfdisk             // fdisk for GPT
   ntfs-3g              // to enable mounting of NTFS write(default will be readonly)

Other file managers:

   mc
   ranger     // Another console, powered by Vim keys

Archives:

   file-roller // Archiver for GNOME

   p7zip       // 7z support
   zip         // ZIP support
   unzip       // ZIP support
   arj         // ARJ support
   lzop        // LZOP support
   cpio        // RPM support
   unrar       // RAR support
   unarj
   unace
   orange
   sharutils

   atool      // console one

Viewers:

   aur/llpp-git   // PDF fast and powerful
   djview         // DJVU
   feh            // image viewer
   eog            // GNOME viewer

   xchm       // CHM viewer
   chmsee     // Modern CHM viewer

   fbreader

   pdfmod     // PDF GUIsh editor
   pdfcrack

Office:

   libreoffice-gnome  // GTK version of Libre Office
   goldendict         // GUIsh dictionary
   sdcv               // Console version of stardict

Ipad support:

   ifuse              // (see setup (4))
   libplist
   libimobiledevice
   libgpod
   usbmuxd

IDE and editors:

   gvim               // The IDE (see setup (5))

   pycharm-community  // Python IDE
   qtcreator          // Nice IDE
   eclipse            // Big and fat but good for Android
   sublime-text-dev   // Don't like currently, but shell give a shot
   monodevelop        // Well good too

C++ compilers and tools:

   gcc                        // GNU compiler
   clang                      // Cool and modern apple compiler
   intel-parallel-studio-2013 // Free for GNU\Linux
   root                       // CERN thing, has interactive cling C++

C++ libs:

   boost

   glm            // Math lib for OpenGL
   glew
   libgl
   libglu
   glfw
   glut
   bullet         // Physics lib
   assimp         // Assets lib
   ogre           // 3D Engine

   mpich          // MPI support
   intel-tdd      // "Parallel STL"

Debuggers:

   rr             // Mozilla debugger for multi-threading
   gdb            // Cool debugger
   cgdb           // "GUI" for it
   valgrind       // Profiler

Assemblers:

   fasm
   fasmarm        // port for ARM
   nasm
   yasm

Reverse:

   aur/darm-git            // disassembly library
   aur/radare2-git         // Reverse engineering tool
   aur/smali               // Android DEX reverse engineering tool
   aur/android-apktool     // Android reverse engineering tool

Build tools:

   make
   cmake

Source code tools:

   ack                      // Better than grep
   the_silver_searcher      // ag - better than ack :)

   colordiff
   meld                     // Nice GUI-sh diff tool
   highlight                // for syntax highlighting in scripts

Version Control:

   git
   tk       // For gitk
   tig      // Console git explorer
   gitg     // GUI-sh git explorer JIC

   mercurial     // Installed for aur only. Should give it a shot.
   subversion

HEX editors:

   dhex
   hte
   hexedit
   ghex   // (see setup(4) for integration)

Emulation:

   virtualbox                 // Virtual (see setup(6))
   virtualbox-host-modules    // Kernel modules
   net-tools                  // Dependency for networking support

   qemu   // Good alternative, TODO: setup later with kickass KVM

Python stuff:

   ipython

   python-numpy
   python-scipy
   aur/python-matplotlib-git  // Building it with latest snapshot
   python2-unidecode
   python-pillow
   python-pygments
   python-pyzmq

   wxpython           // for ipython
   python-nose        // for ipython test suite
   python-pyqt4       // for ipython qt console
   python-sip         // for ipython qt console
   python-pygments    // for ipython qt console
   python-pyzmq       // for ipython notebook and ipython qt console
   python-tornado     // for ipython notebook
   python-jinja       // for ipython notebook
   haskell-pandoc     // ipython notebook conversion

Other Languages:

   racket   // scheme LISP
   mono     // Damn C#
   rust     // Rust
   ruby

   perl
   perl-lwp-protocol-https  // Used for some scripts

Fonts:

   aur/terminus-cyrillic  // Used in virtual terminals

Browsers:

   firefox                 // See setup(7)
   firefox-adblock-plus    // No ads
   firefox-noscript        // No scripts

   chromium                // Used only when damn Flash is required
   chromium-pepper-flash   // With this player it is maintained (flashplugin is not)

Mail:

   thunderbird

Downloaders:

   curl     // New school
   wget     // Old school
   aria2    // Featured
   uget     // GUI-sh

Sharing:

   amule        // e2k still alive?
   qbittorrent  // Easy torrent way
   rtorrent     // Hard way

Instant Messaging:

   pidgin          // for XMPP
   pidgin-latex    // Add-on for LaTeX support
   finch           // Console XMPP
   profanity       // True console XMPP

   irssi     // IRC client
   tox       // for toxing

Audio and sound:

   alsa-utils   // see setup(8)
   alsa-plugin

   qjackctl     // GUI for JACK server
   jack-dbus    // Not sure why this version - TODO: RTFM

Music editors & synth:

   chuck            // Sound coding
   miniaudicle      // Chuck GUI

   supercollider    // For playing around
   pd-extended      // Same. Not only sound, but using as synthesiser

   audacity
   ardour           // Sequencer and editor
   qtracktor        // Another one editor

   linuxsampler     // Sampler engine
   aur/qsampler     // GUI for linuxsampler
   gigedit          // Sample editor

Media players:

   mplayer
   mpv

   mpd
   ncnmcpp

Stream grabbers:

   livestreamer     // Helper script to live streaming
   youtube-dl

Tools:

   ffmpeg
   mkvtoolnix
   mediainfo

Codecs:

   lame     // MP3
   flac     // FLAC
   x264     // x264
   x265

Screen capture:

   scrot
   simplescreenrecorder   // Best recorder ever, like it's OpenGL capture feature

Monitor tools:

   xorg-xrandr
   ddccontrol       // Uses DDC to control brightness (see scripts in ~/bin)
   ddccontrol-db    // Database, no W2600HP yet. TODO: contribute

Sketching:

   xournal-git   // + Add my patches
   mypaint

Editing:

   imagemagick   // Best editor/viewer ever (see setup(16))
   gimp

Books scanning:

   scantailor   // Scankromsator analog
   pdf2djvu     // One click^wcommand converter

Math:

   octave
   gnuplot
   plplot
   sage
   maxima

TeX:

   texlive-most
   texmaker
   lyx

Games:

   gnugo
   gogui


Networking:

   dnsmasq           // Local DNS cache (see setup(9))
   network-manager

Android development:

   aur/android-sdk                  // SDK, installs to /opt/android-sdk
   aur/android-sdk-build-tools      // Build tools (ex. aapt)
   aur/android-sdk-platform-tools   // Platform tools (ex. adb)
   aur/android-platform-19          // Android 4.4
   aur/android-ndk                  // Native support SDK

   android-udev      // Udev rules for most popular devices
   apache-ant        // Ant build system

   lib32-libstdc++   // Android SDK is 32 bit, so using this to workaround
   lib32-libxv       // and this..
   lib32-ncurses     // and this..
   lib32-openal      // ...
   lib32-sdl
   lib32-zlib
   lib32-gcc-libs
   lib32-alsa-lib    // Not sure who is using ALSA there...

   swt  // Another dependency for android-sdk

