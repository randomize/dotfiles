
   Arch Linux setup by randomize

#### Disk partitions #########################################

Number  Start (sector)  End (sector)  Size       Code  Name
1                 34        585936    286.1 MiB   EF00  ESP
2             585937     313085936    149.0 GiB   0700
3          313085937     313585936    244.1 MiB   8300  boot
4          313585937     430773436    55.9 GiB    8300  root
5          430773437     479601561    23.3 GiB    8300  var
6          479601562     518664061    18.6 GiB    8200  swap
7          518664062     625142414    50.8 GiB    8300  home

Filesystem   Size  Used Avail Use% Mounted on  Label
/dev/sda1    286M   49M  238M  17% /boot       boot
/dev/sda2    Not mounted
/dev/sda3    Not mounted, not used
/dev/sda4     55G   18G   35G  34% /           root
/dev/sda5     24G  9.5G   14G  41% /var        var
/dev/sda6    Swap
/dev/sda7     50G  9.9G   38G  21% /home       home
/dev/sdb1    932G  733G  199G  79% /mnt/TERRA

#### Packets: ###############################################

==== First to install ===

   vi
   sudo
   screen
   bash-completion
   // package auto-search hook on "command not found"
   openssh


=== UEFI boot =============

   efibootmgr
   gummiboot

=== Pacman and Aur =====================================

   pacman.conf => enable "Color" "TotalDownload"

   yaourt // Build from PKGBUILD

   namcap // checking packages

   makepkg.conf => set -j9 // to compile multithreaded

==== Video ========================

   intel-dri
   xf86-video-intel
   libva-intel-driver
   mesa-demos          // Beloved glxgears is here

   Set KMS (add hook and mkinitcpo -p linux)

=== Xorg stuff ==========================

   xorg-server
   xorg-xinit

   xf86-input-wacom
   xorg-xev
   xorg-xprop    // Helper utility for windows info
   xvinfo        // Helper for lightson script
   xclip         // For scripting with clipboard

=== Diagnostics  ==========================

   htop
   sysdig

=== DE ==========================

Window manager:

   awesome         
   compton-git     
   libnotify       

   //parcellite    // clipboard manager good and lite // Has problems with nautilus Cut/Copy/Paste

Console and tools:

   rxvt-unicode
   tmux
   zsh
   zsh-syntax-highlighting

   unp                    // Console universal unpacker
   par2cmdline            // Par2 utility
   rmlint                 // Duplicate files searcher and remover

   // Pipe tools
   pv                     // echo "You can simulate on-screen typing just like in the movies" | pv -qL 10

Nautilus:

   nautilus
   libnautilus-extension
   python2-nautilus

   nautilus-actions        // For custom actions in context menus
   nautilus-open-terminal  // To open current dir in TERM
   nautilus-compare        // Integrates meld into context menu
   nautilus-sendto         // Send to pidgin and other places

   sushi                   // Quick preview on space

   Backspace Fix : Add 'gtk_accel_path "<Actions>/ShellActions/Up" "BackSpace"' to ~/.config/nautilus/accels
   Disk usage:     Add 'Disk usage' to actions, with 'baobab %M' command

   gvfs
   gvfs-smp
   gvfs-mtp
   gvfs-gphoto2 + libgphoto2
   dosfstools           // For old FAT etc
   reiserfsprogs        // Reiser formatting / mounting
   gptfdisk             // fdisk for GPT
   ntfs-3g              // to enable mounting of NTFS write(default will be readonly)

   file-roller // Archiver
   zip         // ZIP support
   unzip       // ZIP support
   p7zip       // 7z support
   arj         // ARJ support
   lzop        // LZOP support
   cpio        // RPM support
   unrar       // RAR support
   unarj
   unace
   orange
   sharutils

Other file managers:

   mc
   ranger     // Another console, powered by vim keys
   highlight  // for syntax
   atool      // archives work

Viewers:

   llpp-git   // PDF fast and powerfull
   djview     // DJVU 
   feh        // Images
   eog        // Gnomes viewer

   xchs
   chssee
   fbreader
   pdfmod
   pdfcrack

Office:

   libreoffice-gnome  // GTK version of Libre Office
   goldendict         // GUIsh dictionary
   sdcv               // Console version of stardict

===== Ipad support =================

   1) install:

      libplist libimobiledevice libgpod usbmuxd ifuse

   2) Setup:
       # idevice_id --list  // will show some hash of my iPad
       # ideviceinfo        // will show more random data on iPad
       # idevicepair pair
       # idevicepair validate
       # mkdir /mnt/ipad
       # chown randy /mnt/ipad
       # chgrp users /mnt/ipad
       # gpasswd -a randy usbmux
       $ ifuse /mnt/ipad
       $ /* concrete app */ ifuse --appid com.olimsoft.oplayer.hd.lite /mnt/ipad
       $ /* all root jailed */ ifuse --root /mnt/ipad
       $ ...
       $ umount /mnt/ipad

=== Coding ============

IDE and editors:

   gvim
   sublime-text-dev
   pycharm-community
   qtcreator

Gvim setup:

   1) git clone https://github.com/gmarik/vundle.git ~/.vim/bundle/vundle
   2) launch, :BundleInstall
   3) cd ~/.vim/bundle/vimproc.vim/
   4) make
   5) cd ~/.vim/bundle/YouCompleteMe/
   6) ./install.sh --clang-completer --system-libclang
   7) install python2-unidecode for ultisnips

C++ compilers and tools:

   gcc                        // GNU compiler
   clang                      // Cool and modern apple compiler
   intel-parallel-studio-2013 // Free for linuxes
   root                       // CERN thing, has interactive cling C++

C++ libs:

   boost

   glm                        // Math lib for opengl
   glew
   libgl
   libglu
   glfw
   glut
   bullet                     // Psysics lib
   assimp                     // Assets lib
   ogre                       // 3D Engine

   mpich                      // MPI support
   intel-tdd                  // "Parallel STL"

Debuggers:

   rr                       // Mozilla debugger for multithreading
   gdb                      // Cool debugger
   cgdb                     // "GUI" for it
   valgrind                 // Profiler

Assemblers:

   fasm
   fasmarm   // port for ARM
   nasm
   yasm

Reverse:

   darm-git       // disasm library
   radare2

Build control:

   make
   cmake

Source code tools:

   ack                      // Better than grep
   the_silver_searcher(ag)  // Better than ack :)

   colordiff
   meld                     // Nice GUIsh diff tool

Version Control:

   git
   tk       // For gitk
   tig      // Console git browser
   gitg     // GUIsh git browser JIC

   mercurial
   subversion

HEXers:

   dhex - I like it
   hte
   hexedit
   ghex

Emulation:

   qemu

   virtualbox
   virtualbox-host-modules
   net-tools

   # echo vboxdrv > /etc/modules-load.d/virtualbox.conf
   # gpasswd -a randy vboxusers

Python stuff:

   ipython

   python-numpy
   python-matplotlib-git // Better build from source - without webkit stuff - note python verion in PKGBUILD packaget() routine
   python-pillow
   python-scipy
   python-pygments
   python-pyzmq

   wxpython           needed for ipythonx and ipython-wx
   python-nose        if you want to run IPython's test suite
   python-pyqt4       for ipython qtconsole
   python-sip         for ipython qtconsole
   python-pygments    for ipython qtconsole
   python-pyzmq       for ipython notebook and ipython qtconcole
   python-tornado     for ipython notebook
   python-jinja       for ipython notebook
   haskell-pandoc     ipython notebook conversion

Other Languages:

   racket   // scheme LISP
   mono     // Damn C#
   rust     // Rust
   ruby

==== Fonts =======================

   Install infinality pathces via repository

   otf-brill
   otf-neris
   ttf-aller
   ttf-envy-code-r

   terminus-cyrillic


=== Internet things =========

Browsers:

   firefox
   firefox-adblock-plus
   firefox-noscript

   chromium
   chromium-pepper-flash

Firefox setup 
   ( about:config ) network.standard-url.escape-utf8 = false

Firefox plugins:
   // TODO:

Mail:

   thunderbird

Downloaders:

   curl
   wget
   aria2
   uget

Sharing:

   amule
   qbittorrent

Instant Messaging:

   pidgin
   pidgin-latex

   irssi     // IRC client
   profanity // XMPP client
   tox

==== Audio and sound =============

Drivers:

   alsa-utils
   alsa-plugin

   qjackctl
   jack-dbus

Setup:

   Test:
   speaker-test -c 2
   aplay --list
   alsamixer

   cat /etc/modprobe.d/alsa-base.conf

      # Created by randy to define order of cards

      # First goes Intel PCH
         options snd-hda-intel index=0 model=auto vid=8086 pid=8c20

      # HDMI is the second
         options snd-hda-intel index=1 model=auto vid=8086 pid=0c0c

   This setup will force order of sound cards
   to see cards list use:
      cat /proc/asound/modules   // Note they can use same module for misc H/W
      lspci -nn | grep -i audio  // Tip. Order of output matches detection order - use this fact.
      cat /proc/asound/cards     // Lists detected cards order
      aplay -l                   // Lists avaliable outputs

Music editors & synth:

   chuck            // Sound coding
   miniaudicle      // Chuck GUI
   /* supercollider */
   pd-extended      // Not only sound, but using as synth

   ardour           // Sequencer and editor
   qtracktor        // Another one editor
   /* audacity */   // Has unwanted deps

   qsampler (aur) + all deps(linuzsample) // Sampler
   gigedit                                // Sample editor

Media players:

   mplayer
   mpv
   mpd
   ncnmcpp

Stream grabbers:

   livestreamer     // Helper script to live streaming
   youtube-dl

Tools:

   ffmpeg
   mkvtoolnix
   mediainfo

Codecs:

   lame     // MP3
   flac     // FLAC
   x264     // x264
   x265

Screen capture:

   scrot ( scrot -t 20 -d 5 )
   simple screen recorder ( aur )

Screen capture for ninjas:

   imagemagick import -window root screenshot.jpg

==== Monitor software ===================

   ddccontrol       // Uses DDC to control brightness (see scripts in ~/bin)
   ddccontrol-db    // Database, no W2600HP yet. TODO: contribute

==== Image processing and editing  ============

Sketching:

   xournal-git   // + Add my patchset
   mypaint
   krita

Editing:

   imagemagick   // Best editor/viewer ever
   gimp

Books scanning:

   scantailor
   pdf2djvu

==== Math =====================

   octave
   gnuplot
   plplot
   sage
   maxima
   mathematica (aur)

=== TeX ============

   texlive-most
   texmaker
   lyx

==== Games =============

   gnugo
   gogui


==== DNS =========================================================================================================

   dnsmasq

   set google DNS in:
     /etc/dnsmasq.conf

   put to /etc/resolv.conf:
     nameserver 127.0.0.1
     nameserver 8.8.8.8

   Enable in network manager
   # echo dns=dnsmasq >> /etc/NetworkManager/NetworkManager.conf

==== Forward journald to /dev/tty12 ==============================================================================

 /etc/systemd/journald.conf

      ForwardToConsole=yes
      TTYPath=/dev/tty12
      MaxLevelConsole=info

 # systemctl restart systemd-journald.

==== Android & cocos ==============================================================================

   1) Apache ant : install package apache-ant
   2) Enable multilib(Google sucks with 32bit SDK), install deps:
        pacman -S lib32-alsa-lib lib32-libstdc++5 lib32-libxv lib32-ncurses lib32-openal lib32-sdl lib32-zlib lib32-gcc-libs swt

        // SOLUTION! build form source adb-git which is 64bit and replace original later

   3) save latest SDK and NDK to ~/android/{sdk,ndk}
   4) launch android app and install packages (SDK, platform and build tools)
   5) setup environment vars for adb - already on ZSH config - check
   6) compile aur's adb-git, but not install: unpack and move adb to ~/android/sdk/platfrom-tools/adb replacing it

   7) Install android-udev rules - currentlty moced from aur
   8) clone cocos to ~/cocos2d-x

============ ipv6 ===============================================================================

   Disable with kernel parameter (see ./global_dotfiles/loader/entries/arch.conf)

===== Man pages ======================================================================================

   # mandb
   Then use search
   $ apropos SOMETHING
   And other
   $ whatis

==== Itmages client ================================================================================

   install perl-lwp-protocol-https
   configure password/login
   use it

==== Auto load to console vt1 =====================================================================

cat /etc/systemd/system/getty@tty1.service.d/autologin.conf
[Service]
Type=simple
ExecStart=
ExecStart=-/usr/bin/agetty --autologin username --noclear %I 38400 linux

